#ifndef CU_FOS_OS_STRING
#define CU_FOS_OS_STRING


int StringLength(char s[]);
char SearchString(char * SreachThis,char * ForThis);
void ReverseString(char s[]);
char * IntToAscii(int n);
char isChar(char a);
char isNumber(char a);

#endif
